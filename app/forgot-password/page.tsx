import Link from "next/link";
import { requestPasswordReset } from "@/app/auth/password-actions";
export default async function ForgotPasswordPage({ searchParams }: { searchParams: Promise<{ sent?: string }> }) { const params = await searchParams; return <main className="grid min-h-screen place-items-center p-6"><section className="w-full max-w-md rounded-2xl border border-slate-100 bg-white p-7 shadow-card"><p className="eyebrow">Account recovery</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-ink">Forgot your password?</h1><p className="mt-2 text-sm leading-6 text-slate-500">Enter your email and we’ll send a secure reset link.</p>{params.sent && <p className="mt-5 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">If an account exists for that email, a reset link has been sent.</p>}<form action={requestPasswordReset} className="mt-7 space-y-4"><label className="block text-sm font-medium text-slate-700">Work email<input name="email" required type="email" className="mt-1.5 w-full rounded-lg border px-3 py-3" /></label><button className="button-primary w-full" type="submit">Email reset link</button></form><p className="mt-5 text-center text-sm"><Link href="/login" className="font-semibold text-electric">Back to login</Link></p></section></main>; }
