import Link from "next/link";
import { resetPassword } from "@/app/auth/password-actions";
export default async function ResetPasswordPage({ searchParams }: { searchParams: Promise<{ token?: string; error?: string }> }) { const params = await searchParams; return <main className="grid min-h-screen place-items-center p-6"><section className="w-full max-w-md rounded-2xl border border-slate-100 bg-white p-7 shadow-card"><p className="eyebrow">Account recovery</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-ink">Set a new password</h1>{params.error && <p className="mt-4 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">This reset link is invalid or has expired.</p>}<form action={resetPassword} className="mt-7 space-y-4"><input type="hidden" name="token" value={params.token || ""} /><label className="block text-sm font-medium text-slate-700">New password<input name="password" required minLength={8} type="password" className="mt-1.5 w-full rounded-lg border px-3 py-3" /></label><button className="button-primary w-full" type="submit">Update password</button></form><p className="mt-5 text-center text-sm"><Link href="/login" className="font-semibold text-electric">Back to login</Link></p></section></main>; }
