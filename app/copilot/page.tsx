import Link from "next/link"; import { redirect } from "next/navigation"; import { auth } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { DashboardSidebar } from "@/components/dashboard-sidebar";
export const dynamic="force-dynamic";
export default async function CopilotPage(){const s=await auth();if(!s?.user?.email)redirect("/login");const u=await prisma.user.findUnique({where:{email:s.user.email}});if(!u)redirect("/login");const overdue=await prisma.invoice.findMany({where:{companyId:u.companyId,status:"OVERDUE"},include:{customer:true},orderBy:{amount:"desc"},take:5});return <main className="flex min-h-screen"><DashboardSidebar/><div className="min-w-0 flex-1"><header className="border-b bg-white px-5 py-4 sm:px-8"><p className="text-sm text-slate-500">Owner dashboard</p><h1 className="text-xl font-bold text-ink">CreditPilot Copilot</h1></header><div className="mx-auto max-w-3xl p-5 sm:p-8"><section className="rounded-xl border bg-white p-6 shadow-card"><h2 className="text-2xl font-bold text-ink">Your collection assistant</h2><p className="mt-2 text-slate-600">You currently have {overdue.length} overdue invoice{overdue.length===1?"":"s"} requiring attention.</p><h3 className="mt-7 font-bold text-ink">Priority customers</h3><div className="mt-3 divide-y">{overdue.map(i=><div key={i.id} className="flex justify-between py-3"><span>{i.customer.name}</span><span className="font-semibold">£{Number(i.amount).toLocaleString("en-GB",{minimumFractionDigits:2})}</span></div>)}</div><p className="mt-6 rounded-lg bg-sky p-4 text-sm text-slate-600">Ask questions about overdue invoices, payments, customers, and reminders. AI answers will be connected next.</p><Link href="/dashboard" className="button-primary mt-5 inline-block">Back to dashboard</Link></section></div></div></main>}
