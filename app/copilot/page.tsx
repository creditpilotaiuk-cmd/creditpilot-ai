import { redirect } from "next/navigation"; import { auth } from "@/lib/auth"; import { prisma } from "@/lib/prisma"; import { DashboardSidebar } from "@/components/dashboard-sidebar"; import { askCopilot } from "./actions";
export const dynamic="force-dynamic";
export default async function CopilotPage({searchParams}:{searchParams:Promise<{answer?:string;question?:string;error?:string}>}){const s=await auth();if(!s?.user?.email)redirect("/login");const u=await prisma.user.findUnique({where:{email:s.user.email}});if(!u)redirect("/login");const p=await searchParams;return <main className="flex min-h-screen"><DashboardSidebar/><div className="min-w-0 flex-1"><header className="border-b bg-white px-5 py-4 sm:px-8"><p className="text-sm text-slate-500">Owner dashboard</p><h1 className="text-xl font-bold text-ink">CreditPilot Copilot</h1></header><div className="mx-auto max-w-3xl p-5 sm:p-8"><section className="rounded-xl border bg-white p-6 shadow-card"><h2 className="text-2xl font-bold text-ink">Ask a question</h2><p className="mt-2 text-slate-600">Ask about your invoices, customers, payments, or reminders.</p><form action={askCopilot} className="mt-6 flex gap-3"><input name="question" required defaultValue={p.question} placeholder="Who owes me the most?" className="flex-1 rounded-lg border px-3 py-3"/><button className="button-primary" type="submit">Ask Copilot</button></form>{p.error&&<p className="mt-4 text-sm text-rose-600">Add OPENAI_API_KEY in Vercel first.</p>}{p.answer&&<div className="mt-6 rounded-lg bg-sky p-5"><p className="text-sm font-semibold text-electric">Copilot answer</p><p className="mt-2 whitespace-pre-line text-slate-700">{p.answer}</p></div>}<div className="mt-6 text-sm text-slate-500">Try: “Which invoices are overdue?” or “Who should I chase today?”</div></section></div></div></main>}
